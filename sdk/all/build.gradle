apply plugin: "org.jetbrains.kotlin.jvm"

group = "${rootProject.group}.sdk"
version = rootProject.version

sourceCompatibility = rootProject.jvm_version

dependencies {
    compile project(":core:base")
    compile project(":core:client")
    compile project(":core:server")
}

apply from: rootProject.file("./gradle/common.gradle").absolutePath
apply from: rootProject.file("./gradle/sdk.gradle").absolutePath