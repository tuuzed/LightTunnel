sourceCompatibility = rootProject.jvm_version
targetCompatibility = rootProject.jvm_version

compileKotlin {
    kotlinOptions {
        freeCompilerArgs = ["-Xjsr305=strict"]
        jvmTarget = rootProject.jvm_version
    }
}

compileTestKotlin {
    kotlinOptions {
        freeCompilerArgs = ["-Xjsr305=strict"]
        jvmTarget = rootProject.jvm_version
    }
}

